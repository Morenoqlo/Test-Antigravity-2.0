# Mejoras V3 Portafolio Adesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Adjust magnifier to rectangular shape with more zoom, equalize location icon visual weight, and clean up the footer (bigger logo, no phrase).

**Architecture:** 3 files touched. Task 1 covers all CSS edits (style.css). Task 2 updates the JS magnifier variables (app.js). Task 3 removes the footer phrase (index.html). No git repo — skip all commits.

**Tech Stack:** Vanilla CSS3, Vanilla JS (ES6), HTML5.

**Spec:** `docs/superpowers/specs/2026-05-22-mejoras-v3-design.md`

---

## File Map

| File | Task | Change |
|---|---|---|
| `css/style.css` | 1 | `.img-lens` shape/size, `.fa-location-dot` size, `.footer-logo-img` size |
| `js/app.js` | 2 | `lensW`, `lensH`, `zoomFactor` + position/background calculations |
| `index.html` | 3 | Remove `<p>` from `.footer-brand` |

---

### Task 1: CSS — Tres ediciones en style.css

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css`

- [ ] **Step 1: Actualizar .img-lens — rectangular, más grande**

  Localizar el selector `.img-lens` (al final del archivo, dentro del bloque `/* LUPA MAGNIFIER */`).
  
  Actualmente tiene:
  ```css
  .img-lens {
      position: absolute;
      width: 130px;
      height: 130px;
      border-radius: 50%;
      border: 2px solid var(--color-accent-1);
      box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.5),
                  0 8px 24px rgba(0, 0, 0, 0.6),
                  0 0 15px rgba(255, 184, 0, 0.3);
      display: none;
      pointer-events: none;
      z-index: 20;
      background-repeat: no-repeat;
  }
  ```
  
  Cambiar solo `width`, `height` y `border-radius`:
  ```css
  .img-lens {
      position: absolute;
      width: 220px;
      height: 160px;
      border-radius: 12px;
      border: 2px solid var(--color-accent-1);
      box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.5),
                  0 8px 24px rgba(0, 0, 0, 0.6),
                  0 0 15px rgba(255, 184, 0, 0.3);
      display: none;
      pointer-events: none;
      z-index: 20;
      background-repeat: no-repeat;
  }
  ```

- [ ] **Step 2: Agregar regla para .fa-location-dot**

  Al final de `style.css`, después del bloque `/* LUPA MAGNIFIER */`, agregar:
  ```css
  /* Equilibrar visualmente el ícono de ubicación */
  .method-item .fa-location-dot {
      font-size: 1.8rem;
  }
  ```

- [ ] **Step 3: Actualizar .footer-logo-img**

  Localizar `.footer-logo-img` en `style.css`. Actualmente:
  ```css
  .footer-logo-img {
      height: 60px !important;
      max-height: 60px !important;
      width: auto !important;
      max-width: 260px !important;
      margin-bottom: 0.5rem;
      display: block !important;
      object-fit: contain !important;
      flex-shrink: 0 !important;
  }
  ```
  
  Reemplazar con:
  ```css
  .footer-logo-img {
      height: 80px !important;
      max-height: 80px !important;
      width: auto !important;
      max-width: 320px !important;
      margin-bottom: 0.5rem;
      display: block !important;
      object-fit: contain !important;
      flex-shrink: 0 !important;
  }
  ```

- [ ] **Step 4: Verificar los 3 cambios CSS**

  Confirmar en `style.css`:
  - `.img-lens` tiene `width: 220px`, `height: 160px`, `border-radius: 12px`
  - `.method-item .fa-location-dot` existe con `font-size: 1.8rem`
  - `.footer-logo-img` tiene `height: 80px !important` y `max-width: 320px !important`

---

### Task 2: JS — Actualizar variables de la lupa

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\js\app.js`

- [ ] **Step 1: Leer el bloque actual del magnifier en app.js**

  El bloque `/* 10. LUPA MAGNIFIER EN MODAL */` contiene actualmente (dentro del `mousemove` listener):
  ```js
  const lensSize = 130;
  const zoomFactor = 2.5;

  lens.style.display = 'block';
  modalVisualEl.classList.add('lens-active');

  const containerRect = modalVisualEl.getBoundingClientRect();
  lens.style.left = (e.clientX - containerRect.left - lensSize / 2) + 'px';
  lens.style.top  = (e.clientY - containerRect.top  - lensSize / 2) + 'px';

  lens.style.backgroundImage    = `url('${img.src}')`;
  lens.style.backgroundSize     = `${rect.width * zoomFactor}px ${rect.height * zoomFactor}px`;
  lens.style.backgroundPosition =
      `-${x * zoomFactor - lensSize / 2}px -${y * zoomFactor - lensSize / 2}px`;
  ```

- [ ] **Step 2: Reemplazar con variables lensW/lensH y nuevo zoomFactor**

  Reemplazar ese bloque (desde `const lensSize` hasta el cierre del `backgroundPosition`) con:
  ```js
  const lensW = 220;
  const lensH = 160;
  const zoomFactor = 3.5;

  lens.style.display = 'block';
  modalVisualEl.classList.add('lens-active');

  const containerRect = modalVisualEl.getBoundingClientRect();
  lens.style.left = (e.clientX - containerRect.left - lensW / 2) + 'px';
  lens.style.top  = (e.clientY - containerRect.top  - lensH / 2) + 'px';

  lens.style.backgroundImage    = `url('${img.src}')`;
  lens.style.backgroundSize     = `${rect.width * zoomFactor}px ${rect.height * zoomFactor}px`;
  lens.style.backgroundPosition =
      `-${x * zoomFactor - lensW / 2}px -${y * zoomFactor - lensH / 2}px`;
  ```

  **Verificar:** La variable `lensSize` ya NO debe existir en el archivo. Solo `lensW` y `lensH`.

- [ ] **Step 3: Verificar estructura de app.js**

  Confirmar:
  - `lensSize` ya no existe en `app.js`
  - `lensW = 220` y `lensH = 160` están en el listener `mousemove`
  - `zoomFactor = 3.5` (no 2.5)
  - `backgroundPosition` usa `lensW` para X y `lensH` para Y
  - El archivo termina con `});` como última línea

---

### Task 3: HTML — Eliminar frase del footer

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\index.html`

- [ ] **Step 1: Eliminar el párrafo de .footer-brand**

  Localizar en `index.html`:
  ```html
  <div class="footer-brand">
      <img src="assets/logo.png" alt="Adesign Logo" class="footer-logo-img">
      <p>Estética impecable y rigor visual, diseñando marcas y empaques memorables con visión innovadora.</p>
  </div>
  ```
  
  Reemplazar con:
  ```html
  <div class="footer-brand">
      <img src="assets/logo.png" alt="Adesign Logo" class="footer-logo-img">
  </div>
  ```

- [ ] **Step 2: Verificar**

  Confirmar que `.footer-brand` en `index.html` solo contiene el `<img>` y nada más. El texto "Estética impecable" ya no debe aparecer.

---

## Verificación final

- [ ] Abrir `http://localhost:3001` y abrir cualquier proyecto
- [ ] Pasar el mouse sobre la imagen del modal — la lupa debe ser rectangular (220×160px, esquinas redondeadas), con zoom notablemente mayor (3.5×)
- [ ] Ir a sección Contacto — el ícono de ubicación (📍) debe verse visualmente equivalente a WhatsApp y Email
- [ ] Ir al Footer — logo más grande (80px), sin frase debajo

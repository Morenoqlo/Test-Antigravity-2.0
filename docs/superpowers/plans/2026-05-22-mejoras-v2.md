# Mejoras V2 Portafolio Adesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Apply 5 targeted improvements to the Adesign portfolio: clean up the hero badge, replace the code card with a design process card, add a magnifier lens to the project modal, rename a section title, and fix the footer logo duplication.

**Architecture:** Changes span 3 files (index.html, css/style.css, js/app.js). Tasks 1 and 3 are HTML-only. Task 2 touches HTML + CSS. Task 4 is CSS-only. Task 5 is the most complex — CSS + JS for the magnifier feature. No git repository exists — skip all commit steps.

**Tech Stack:** Vanilla HTML5, CSS3 (custom properties), Vanilla JavaScript (ES6), Font Awesome 6, Google Fonts.

**Spec:** `docs/superpowers/specs/2026-05-22-mejoras-v2-design.md`

---

## File Map

| File | Tasks | Responsibility |
|---|---|---|
| `index.html` | 1, 2 | Badge text, hero card HTML, section title, footer HTML |
| `css/style.css` | 2, 3, 4 | Hero card CSS classes, footer logo size, magnifier CSS |
| `js/app.js` | 5 | Magnifier lens logic |

---

### Task 1: Ediciones HTML simples — badge, título sección, footer

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\index.html`

- [ ] **Step 1: Eliminar emoji ⚡ del badge**

  Localizar en `index.html`:
  ```html
  <div class="badge" id="hero-badge">⚡ Disponible para nuevos proyectos</div>
  ```
  Reemplazar con:
  ```html
  <div class="badge" id="hero-badge">Disponible para nuevos proyectos</div>
  ```

- [ ] **Step 2: Renombrar h2 de la sección Trayectoria**

  Localizar en `index.html`:
  ```html
  <h2 class="section-title">Hitos y Evolución</h2>
  <p class="section-subtitle">Mi trayectoria profesional en diseño gráfico, identidad visual, gestión de marca y comunicaciones.</p>
  ```
  Reemplazar con:
  ```html
  <h2 class="section-title">Experiencia</h2>
  <p class="section-subtitle">Empresas, proyectos y formación que han marcado mi camino.</p>
  ```

- [ ] **Step 3: Eliminar span duplicado en el footer**

  Localizar en `index.html` (sección footer):
  ```html
  <div class="footer-brand">
      <img src="assets/logo.png" alt="Adesign Logo" class="footer-logo-img">
      <span class="logo-text">Adesign</span>
      <p>Estética impecable y rigor visual, diseñando marcas y empaques memorables con visión innovadora.</p>
  </div>
  ```
  Reemplazar con:
  ```html
  <div class="footer-brand">
      <img src="assets/logo.png" alt="Adesign Logo" class="footer-logo-img">
      <p>Estética impecable y rigor visual, diseñando marcas y empaques memorables con visión innovadora.</p>
  </div>
  ```

- [ ] **Step 4: Verificar los 3 cambios**

  Leer las líneas modificadas de `index.html` y confirmar:
  - El badge dice exactamente `Disponible para nuevos proyectos` sin emoji
  - El h2 de la sección dice exactamente `Experiencia`
  - El subtítulo de la sección dice `Empresas, proyectos y formación que han marcado mi camino.`
  - El `.footer-brand` ya NO tiene el `<span class="logo-text">Adesign</span>`

---

### Task 2: Tarjeta Hero — Proceso Creativo (HTML + CSS)

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\index.html`
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css`

- [ ] **Step 1: Reemplazar contenido HTML de la hero card**

  Localizar en `index.html` el bloque completo:
  ```html
  <div class="glass-card hero-card" id="hero-interactive-card">
      <div class="hero-card-header">
          <span class="dot red"></span>
          <span class="dot yellow"></span>
          <span class="dot green"></span>
          <span class="terminal-title">adesign_process.js</span>
      </div>
      <div class="hero-card-body">
          <p class="code-line"><span class="code-keyword">const</span> creativeProcess = {</p>
          <p class="code-line indent-1"><span class="code-property">brand</span>: <span class="code-string">"Adesign"</span>,</p>
          <p class="code-line indent-1"><span class="code-property">creator</span>: <span class="code-string">"Alexander Rojas"</span>,</p>
          <p class="code-line indent-1"><span class="code-property">specialty</span>: [<span class="code-string">"Visual Identity"</span>, <span class="code-string">"IA Direction"</span>],</p>
          <p class="code-line indent-1"><span class="code-property">philosophy</span>: <span class="code-string">"Estética impecable + Foco estratégico"</span>,</p>
          <p class="code-line indent-1"><span class="code-property">goal</span>: <span class="code-string">"Comunicar mejor para conectar marcas"</span></p>
          <p class="code-line">};</p>
          
          <div class="hero-card-decoration">
              <i class="fa-solid fa-compass-drafting glowing-icon"></i>
              <i class="fa-solid fa-pen-nib glowing-icon"></i>
              <i class="fa-solid fa-cube glowing-icon"></i>
          </div>
      </div>
  </div>
  ```

  Reemplazar con:
  ```html
  <div class="glass-card hero-card" id="hero-interactive-card">
      <div class="hero-card-header">
          <span class="dot red"></span>
          <span class="dot yellow"></span>
          <span class="dot green"></span>
          <span class="terminal-title">proceso_creativo.flow</span>
      </div>
      <div class="hero-card-body">
          <div class="process-step">
              <div class="process-step-num">01</div>
              <div class="process-step-content">
                  <span class="process-step-label">Concepto & Brief</span>
                  <span class="process-step-desc">Investigación y definición estratégica</span>
              </div>
          </div>
          <div class="process-connector"></div>
          <div class="process-step">
              <div class="process-step-num">02</div>
              <div class="process-step-content">
                  <span class="process-step-label">Boceto & Dirección</span>
                  <span class="process-step-desc">Exploración visual y selección de ruta</span>
              </div>
          </div>
          <div class="process-connector"></div>
          <div class="process-step">
              <div class="process-step-num">03</div>
              <div class="process-step-content">
                  <span class="process-step-label">Producción Digital</span>
                  <span class="process-step-desc">Vectorización, 3D y composición final</span>
              </div>
          </div>
          <div class="process-connector"></div>
          <div class="process-step">
              <div class="process-step-num">04</div>
              <div class="process-step-content">
                  <span class="process-step-label">Entrega & Manual</span>
                  <span class="process-step-desc">Archivos maestros y guía de aplicación</span>
              </div>
          </div>
      </div>
  </div>
  ```

- [ ] **Step 2: Agregar clases CSS del proceso creativo al final de `style.css`**

  Agregar al final de `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css`:

  ```css
  /* -------------------------------------------------------------
     HERO CARD: PROCESO CREATIVO
  ------------------------------------------------------------- */
  .process-step {
      display: flex;
      align-items: center;
      gap: 1rem;
  }

  .process-step-num {
      width: 32px;
      height: 32px;
      border-radius: 50%;
      background: var(--grad-primary);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 0.7rem;
      font-weight: 800;
      color: #050508;
      flex-shrink: 0;
      font-family: var(--font-heading);
      letter-spacing: 0;
  }

  .process-step-content {
      display: flex;
      flex-direction: column;
      gap: 0.15rem;
  }

  .process-step-label {
      font-size: 0.9rem;
      font-weight: 700;
      color: var(--text-primary);
      font-family: var(--font-heading);
  }

  .process-step-desc {
      font-size: 0.72rem;
      color: var(--text-muted);
      line-height: 1.3;
  }

  .process-connector {
      width: 2px;
      height: 14px;
      background: var(--border-glass);
      margin: 4px 0 4px 15px;
  }
  ```

- [ ] **Step 3: Verificar**

  - Confirmar que el bloque HTML en `index.html` ya NO contiene `code-line`, `code-keyword`, ni `code-string`
  - Confirmar que `style.css` termina con el bloque `/* HERO CARD: PROCESO CREATIVO */`
  - Confirmar que `.process-connector` tiene `margin: 4px 0 4px 15px` (el `15px` alinea la línea con el centro del círculo de 32px)

---

### Task 3: Footer logo CSS

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css`

- [ ] **Step 1: Verificar estado actual de `.footer-logo-img`**

  Buscar en `style.css`:
  ```css
  .footer-logo-img {
      height: 48px !important;
      max-height: 48px !important;
      width: auto !important;
      max-width: 200px !important;
      margin-bottom: 0.5rem;
      display: block !important;
      object-fit: contain !important;
      flex-shrink: 0 !important;
  }
  ```

- [ ] **Step 2: Actualizar tamaño del logo en footer**

  Reemplazar con:
  ```css
  .footer-logo-img {
      height: 60px !important;
      max-height: 60px !important;
      width: auto !important;
      max-width: 260px !important;
      margin-bottom: 0.5rem;
      display: block !important;
      object-fit: contain !important;
      flex-shrink: 0 !important;
  }
  ```

- [ ] **Step 3: Verificar**

  Confirmar que `style.css` tiene `height: 60px !important` y `max-width: 260px !important` en `.footer-logo-img`.

---

### Task 4: Magnifier Lens — CSS + JS

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css`
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\js\app.js`

- [ ] **Step 1: Agregar CSS de la lupa al final de `style.css`**

  Agregar al final de `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css` (después del bloque de proceso creativo):

  ```css
  /* -------------------------------------------------------------
     LUPA MAGNIFIER — MODAL DE PROYECTOS
  ------------------------------------------------------------- */
  .img-lens {
      position: absolute;
      width: 130px;
      height: 130px;
      border-radius: 50%;
      border: 2px solid var(--color-accent-1);
      box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.5),
                  0 8px 24px rgba(0, 0, 0, 0.6),
                  0 0 15px rgba(255, 184, 0, 0.3);
      display: none;
      pointer-events: none;
      z-index: 20;
      background-repeat: no-repeat;
  }

  .modal-visual.lens-active {
      cursor: none;
  }
  ```

- [ ] **Step 2: Agregar JS de la lupa al final de `app.js`**

  El archivo `js/app.js` envuelve todo en `document.addEventListener('DOMContentLoaded', () => { ... })`. Hay que agregar el bloque del magnifier **dentro** de ese listener, justo antes del cierre `});` final (última línea del archivo).

  Localizar el cierre final del archivo:
  ```js
  });
  ```
  
  Reemplazar esa última línea con:
  ```js
      /* ==========================================
         10. LUPA MAGNIFIER EN MODAL
         ========================================== */
      const lens = document.createElement('div');
      lens.className = 'img-lens';
      lens.id = 'img-lens';
      document.querySelector('.modal-visual').appendChild(lens);

      const modalVisualEl = document.querySelector('.modal-visual');

      modalVisualEl.addEventListener('mousemove', (e) => {
          const img = document.getElementById('modal-project-img');
          if (!img || !img.src || img.style.opacity === '0') {
              lens.style.display = 'none';
              return;
          }

          const rect = img.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;

          if (x < 0 || y < 0 || x > rect.width || y > rect.height) {
              lens.style.display = 'none';
              modalVisualEl.classList.remove('lens-active');
              return;
          }

          const lensSize = 130;
          const zoomFactor = 2.5;

          lens.style.display = 'block';
          modalVisualEl.classList.add('lens-active');

          const containerRect = modalVisualEl.getBoundingClientRect();
          lens.style.left = (e.clientX - containerRect.left - lensSize / 2) + 'px';
          lens.style.top  = (e.clientY - containerRect.top  - lensSize / 2) + 'px';

          lens.style.backgroundImage    = `url('${img.src}')`;
          lens.style.backgroundSize     = `${rect.width * zoomFactor}px ${rect.height * zoomFactor}px`;
          lens.style.backgroundPosition =
              `-${x * zoomFactor - lensSize / 2}px -${y * zoomFactor - lensSize / 2}px`;
      });

      modalVisualEl.addEventListener('mouseleave', () => {
          lens.style.display = 'none';
          modalVisualEl.classList.remove('lens-active');
      });

  });
  ```

  **IMPORTANTE:** El bloque anterior debe quedar **dentro** del `DOMContentLoaded`. La última línea del archivo debe seguir siendo `});` (el cierre del listener). Verificar que no hay `});` duplicados al final.

- [ ] **Step 3: Verificar estructura de app.js**

  Confirmar:
  - El bloque `/* 10. LUPA MAGNIFIER */` está presente en `app.js`
  - `document.querySelector('.modal-visual').appendChild(lens)` aparece después de `DOMContentLoaded`
  - El archivo termina con exactamente un `});` en la última línea (el cierre del listener)
  - No hay código fuera del `DOMContentLoaded` listener

- [ ] **Step 4: Verificar comportamiento visual**

  Abrir el sitio en `http://localhost:3001`, abrir cualquier proyecto (click en "Explorar"), y pasar el mouse sobre la imagen en el lado izquierdo del modal. Confirmar:
  - Aparece una lupa circular con borde amarillo
  - La zona bajo el cursor se muestra ampliada dentro de la lupa
  - Al salir del área de la imagen la lupa desaparece
  - El cursor del mouse desaparece cuando está sobre la imagen (reemplazado por la lupa)

---

## Verificación final

- [ ] Abrir `http://localhost:3001` y revisar el Hero — el badge dice "Disponible para nuevos proyectos" sin rayo, la tarjeta derecha muestra los 4 pasos del proceso creativo
- [ ] Bajar a la sección de Trayectoria — el título dice "Experiencia"
- [ ] Abrir cualquier proyecto → verificar lupa en la imagen del modal
- [ ] Bajar al Footer — solo aparece el logo una vez (sin texto "Adesign" duplicado debajo), y el logo se ve proporcionado (60px)

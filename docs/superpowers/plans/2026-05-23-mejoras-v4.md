# Mejoras V4 Portafolio Adesign — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the magnifier lens with a full-width scrollable image in the project modal. This solves the root cause: tall screenshots rendered narrow by `object-fit: contain` at ~150px wide made any zoom useless.

**Architecture:** 2 files touched. Task 1 edits style.css (4 sub-changes). Task 2 removes the magnifier section from app.js. No HTML changes. No git repo — skip all commits.

**Tech Stack:** Vanilla CSS3, Vanilla JS (ES6), HTML5.

**Spec:** `docs/superpowers/specs/2026-05-23-mejoras-v4-design.md`

---

## File Map

| File | Task | Change |
|---|---|---|
| `css/style.css` | 1 | `.modal-visual` overflow+align, `#modal-project-img` full-width, custom scrollbar, remove lens CSS |
| `js/app.js` | 2 | Remove section 10 (LUPA MAGNIFIER) entirely |

---

### Task 1: CSS — Cuatro ediciones en style.css

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\css\style.css`

- [ ] **Step 1: Modificar `.modal-visual` — overflow y align**

  Localizar el selector `.modal-visual` (línea ~1438, bloque "Contenido Visual Modal"):

  Actualmente:
  ```css
  .modal-visual {
      position: relative;
      height: 100%;
      min-height: 350px;
      overflow: hidden;
      background: #040406;
      display: flex;
      align-items: center;
      justify-content: center;
  }
  ```

  Reemplazar con:
  ```css
  .modal-visual {
      position: relative;
      height: 100%;
      min-height: 350px;
      overflow-y: auto;
      overflow-x: hidden;
      background: #040406;
      display: flex;
      align-items: flex-start;
      justify-content: center;
  }
  ```

- [ ] **Step 2: Agregar scrollbar personalizada inmediatamente después de `.modal-visual`**

  Justo después del bloque `.modal-visual { ... }` (antes de `.modal-visual-blur`), insertar:
  ```css
  /* Scrollbar personalizada para modal visual */
  .modal-visual::-webkit-scrollbar {
      width: 4px;
  }
  .modal-visual::-webkit-scrollbar-track {
      background: transparent;
  }
  .modal-visual::-webkit-scrollbar-thumb {
      background: rgba(255, 184, 0, 0.3);
      border-radius: 2px;
  }
  .modal-visual::-webkit-scrollbar-thumb:hover {
      background: rgba(255, 184, 0, 0.6);
  }
  ```

- [ ] **Step 3: Modificar `#modal-project-img` — imagen a ancho completo**

  Localizar `#modal-project-img` (línea ~1464):

  Actualmente:
  ```css
  #modal-project-img {
      max-width: 90%;
      max-height: 85%;
      width: auto;
      height: auto;
      object-fit: contain;
      display: block;
      z-index: 2;
      position: relative;
      border-radius: 8px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
      transition: opacity 0.3s ease;
  }
  ```

  Reemplazar con:
  ```css
  #modal-project-img {
      max-width: 100%;
      max-height: none;
      width: 100%;
      height: auto;
      object-fit: unset;
      display: block;
      z-index: 2;
      position: relative;
      border-radius: 8px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
      transition: opacity 0.3s ease;
  }
  ```

- [ ] **Step 4: Eliminar bloque LUPA MAGNIFIER**

  Localizar y eliminar completamente el bloque (líneas ~1942-1962):
  ```css
  /* -------------------------------------------------------------
     LUPA MAGNIFIER — MODAL DE PROYECTOS
  ------------------------------------------------------------- */
  .img-lens {
      position: absolute;
      width: 220px;
      height: 160px;
      border-radius: 12px;
      border: 2px solid var(--color-accent-1);
      box-shadow: 0 0 0 2px rgba(0, 0, 0, 0.5),
                  0 8px 24px rgba(0, 0, 0, 0.6),
                  0 0 15px rgba(255, 184, 0, 0.3);
      display: none;
      pointer-events: none;
      z-index: 20;
      background-repeat: no-repeat;
  }

  .modal-visual.lens-active {
      cursor: none;
  }
  ```

  El bloque que sigue — `/* Equilibrar visualmente el ícono de ubicación */` con `.method-item .fa-location-dot` — debe mantenerse intacto.

- [ ] **Step 5: Verificar los 4 cambios CSS**

  Confirmar en `style.css`:
  - `.modal-visual` tiene `overflow-y: auto; overflow-x: hidden` y `align-items: flex-start`
  - Existen los 4 selectores `::webkit-scrollbar` para `.modal-visual`
  - `#modal-project-img` tiene `width: 100%`, `max-height: none`, `object-fit: unset`
  - `.img-lens` ya NO existe en el archivo
  - `.modal-visual.lens-active` ya NO existe en el archivo
  - `.method-item .fa-location-dot { font-size: 1.8rem; }` sigue presente al final

---

### Task 2: JS — Eliminar sección de la lupa

**Files:**
- Modify: `C:\Users\alexa\Downloads\Test Antigravity 2.0\js\app.js`

- [ ] **Step 1: Eliminar el bloque completo de la lupa**

  Localizar en `app.js` el bloque que empieza en la línea ~663:
  ```js
      /* ==========================================
         10. LUPA MAGNIFIER EN MODAL
         ========================================== */
      const lens = document.createElement('div');
      lens.className = 'img-lens';
      lens.id = 'img-lens';
      document.querySelector('.modal-visual').appendChild(lens);

      const modalVisualEl = document.querySelector('.modal-visual');

      modalVisualEl.addEventListener('mousemove', (e) => {
          const img = document.getElementById('modal-project-img');
          if (!img || !img.src || img.style.opacity === '0') {
              lens.style.display = 'none';
              return;
          }

          const rect = img.getBoundingClientRect();
          const x = e.clientX - rect.left;
          const y = e.clientY - rect.top;

          if (x < 0 || y < 0 || x > rect.width || y > rect.height) {
              lens.style.display = 'none';
              modalVisualEl.classList.remove('lens-active');
              return;
          }

          const lensW = 220;
          const lensH = 160;
          const zoomFactor = 3.5;

          lens.style.display = 'block';
          modalVisualEl.classList.add('lens-active');

          const containerRect = modalVisualEl.getBoundingClientRect();
          lens.style.left = (e.clientX - containerRect.left - lensW / 2) + 'px';
          lens.style.top  = (e.clientY - containerRect.top  - lensH / 2) + 'px';

          lens.style.backgroundImage    = `url('${img.src}')`;
          lens.style.backgroundSize     = `${rect.width * zoomFactor}px ${rect.height * zoomFactor}px`;
          lens.style.backgroundPosition =
              `-${x * zoomFactor - lensW / 2}px -${y * zoomFactor - lensH / 2}px`;
      });

      modalVisualEl.addEventListener('mouseleave', () => {
          lens.style.display = 'none';
          modalVisualEl.classList.remove('lens-active');
      });
  ```

  Eliminar **todo ese bloque** (desde el comentario `/* 10. LUPA */` hasta el cierre del `mouseleave` listener inclusive), dejando una línea en blanco antes del `});` final.

- [ ] **Step 2: Verificar estructura de app.js**

  Confirmar:
  - `img-lens`, `lens`, `lensW`, `lensH`, `zoomFactor`, `lens-active` ya NO existen en `app.js`
  - La última línea del archivo sigue siendo `});` (el cierre del `DOMContentLoaded`)
  - No hay código huérfano ni bloques sin cerrar

---

## Verificación final

- [ ] Abrir `http://localhost:3001` y abrir cualquier proyecto
- [ ] Pasar el mouse sobre la imagen del modal — NO debe aparecer ninguna lupa, cursor normal
- [ ] La imagen ocupa el ancho completo del panel izquierdo
- [ ] Para proyectos con screenshots verticales (ej: Vaiss), el contenido es scrolleable dentro del panel
- [ ] El carrusel (flechas prev/next y dots) sigue funcionando
- [ ] El footer, la sección Contacto y el resto del sitio no se ven afectados
